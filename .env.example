# 家庭卧底派对 · 环境变量说明
#
# 本项目是纯前端静态单页应用（Vite + React + TS）：
#   - 运行期零网络请求、零后端依赖（PRD §4 技术边界约束）
#   - 无任何密钥、Token、第三方服务
#   - 战绩数据仅存本机 localStorage
#
# 因此本项目【无需配置任何环境变量】。此文件仅作约定占位。
# 若未来引入需要配置的能力（如自定义词库远程同步，见 README Roadmap），
# 请在此文件登记变量名，并把真实值放入本地 .env（已被 .gitignore 忽略）。
